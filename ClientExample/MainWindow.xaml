<Window x:Class="ClientExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:conv="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
        xmlns:widgets="clr-namespace:ClientExample.Widgets"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Driving Lab Client Example"
        Height="916"
        Width="816"
        MinHeight="458"
        MinWidth="204">

    <UniformGrid Columns="4">
        <widgets:LeapMotion DataContext="{Binding LeapMotion}"/>
        <widgets:SmartEye DataContext="{Binding SmartEye}"/>
        <widgets:SoundPlayer DataContext="{Binding SoundPlayer}"/>
        <widgets:Screen DataContext="{Binding Screen}"/>
        <widgets:Pointing DataContext="{Binding Pointing}"/>
        <widgets:TensionR DataContext="{Binding TensionR}"/>
        <widgets:StreamDeck DataContext="{Binding StreamDeck}"/>
        <widgets:Driving DataContext="{Binding Driving}"/>
    </UniformGrid>
</Window>
