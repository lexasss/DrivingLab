<Window x:Class="ClientExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:conv="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
        xmlns:widgets="clr-namespace:ClientExample.Widgets"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Driving Lab Client Example"
        Height="916"
        Width="1020"
        MinHeight="458"
        MinWidth="204">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <widgets:LeapMotion
            Grid.Row="0"
            Grid.Column="0"
            DataContext="{Binding LeapMotion}" />

        <widgets:SmartEye
            Grid.Row="0"
            Grid.Column="1"
            DataContext="{Binding SmartEye}" />

        <widgets:SoundPlayer
            Grid.Row="0"
            Grid.Column="2"
            DataContext="{Binding SoundPlayer}" />

        <widgets:Screen
            Grid.Row="0"
            Grid.Column="4"
            DataContext="{Binding Screen}" />

        <widgets:Driving
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="3"
            DataContext="{Binding Driving}" />

        <widgets:Pointing
            Grid.Row="1"
            Grid.Column="0"
            DataContext="{Binding Pointing}" />

        <widgets:TensionR
            Grid.Row="1"
            Grid.Column="1"
            DataContext="{Binding TensionR}" />

        <widgets:StreamDeck
            Grid.Row="1"
            Grid.Column="2"
            DataContext="{Binding StreamDeck}" />
    </Grid>

</Window>
