<Window x:Class="ClientExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:conv="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
        xmlns:widgets="clr-namespace:ClientExample.Widgets"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Driving Lab Client Example"
        Height="450"
        Width="800"
        MinHeight="380"
        MinWidth="320">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <widgets:LeapMotion Grid.Column="0"
                            DataContext="{Binding LeapMotion}"/>
        <widgets:SmartEye Grid.Column="1"
                          DataContext="{Binding SmartEye}"/>
        <widgets:SoundPlayer Grid.Column="2"
                          DataContext="{Binding SoundPlayer}"/>
    </Grid>
</Window>
