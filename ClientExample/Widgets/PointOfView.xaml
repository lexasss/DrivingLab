<UserControl x:Class="ClientExample.Widgets.PointOfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientExample.Widgets"
             xmlns:conv="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="80"
             d:DesignWidth="80"
             x:Name="root">
    
    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
    </UserControl.Resources>
    
    <Grid>
        <!-- Inner face -->
        <Ellipse Fill="#E0EFFF"
                 Stroke="#404040"
                 StrokeThickness="1"/>

        <!-- Needle -->
        <Grid RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <RotateTransform Angle="{Binding Value, ElementName=root}"
                                 CenterX="0.5"
                                 CenterY="0.5"/>
            </Grid.RenderTransform>

            <!-- Arrow -->
            <Polygon Points="0,0 0,0.5 0.5,0.5 0.5,1 0.5,0.5 1,0.5 1,0"
                     Fill="Red"
                     Width="3"
                     Stretch="Fill"
                     Visibility="{Binding HasValue, ElementName=root, Converter={StaticResource BooleanToVisibility}}"/>
        </Grid>
    </Grid>
</UserControl>
