syntax = "proto3";

package Common;

message Bool {
	bool value = 1;
}

message Vector {
	double x = 1;
	double y = 2;
	double z = 3;
}

enum Status {
	Disconnected = 0;
	Connected = 1;
	Calibrating = 2;
	Calibrated = 3;
}